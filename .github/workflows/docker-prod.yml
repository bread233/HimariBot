name: Build and Push Docker Image

on:
  workflow_dispatch:       # 手动执行
  push:
    branches:
      - a01                # 仅生产分支触发
    paths-ignore:
      - 'README.md'
      - '.github/workflows/**'
      - 'README*.md'
      - 'docs/**'
      - '.github/ISSUE_TEMPLATE/**'
      - 'docker/docker-compose*.yml'
      - 'docker-compose*.yml'
      - 'resources/**'
      
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read       # 最低权限即可构建镜像

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show workspace size (top dirs)
        run: |
          echo "===== du -h --max-depth=2 . | sort -h | tail -n 30 ====="
          du -h --max-depth=2 . | sort -h | tail -n 30

      - name: Show biggest files
        run: |
          echo "===== du -ah . | sort -h | tail -n 50 ====="
          du -ah . | sort -h | tail -n 50

      - name: Set image tag
        id: tag
        run: |
          VERSION="v${GITHUB_RUN_NUMBER}-${GITHUB_SHA::7}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "🎯 Image Version: $VERSION"

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .          # 构建上下文：项目根目录
          file: Dockerfile    # 使用根目录的 Dockerfile
          push: true
          tags: |
            xmb233/himaribot:${{ env.VERSION }}
            xmb233/himaribot:latest
